# 安全策略

## 支持的版本

| 版本 | 支持状态 |
|------|---------|
| 3.0.x | ✅ 支持 |
| < 3.0 | ❌ 不再支持 |

## 报告漏洞

发现安全漏洞请**不要**在公开 Issue 中提交，通过 GitHub Security Advisories 私密报告：

1. 进入仓库页面 → **Security** → **Report a vulnerability**

或通过 Issue 联系维护者获取其他报告渠道。

请在报告中包含：

- 漏洞类型（如命令注入、越权、DoS）
- 影响范围与复现步骤
- 可能的修复建议

收到报告后将在 7 天内确认，修复后发布新版本并致谢报告者（除非要求匿名）。

## 已知安全设计

- **scriptevent 鉴权**：Endstone 与行为包双向通信携带每次启动重新生成的随机令牌，玩家伪造的 `scriptevent` 消息会被丢弃
- **名称白名单**：假人名称限制 `^[a-zA-Z0-9_-]+$`，防止命令参数注入
- **UUID 所有权**：管理权限优先按 Xbox UUID 判定，名称重用无法继承他人假人
- **NBT 防护**：解析 `level.dat` 时限制递归深度（512）与列表长度（1,000,000），恶意世界文件无法触发栈溢出或内存耗尽
- **原子写入**：`bots.json` 与 `world_behavior_packs.json` 均通过临时文件原子替换，写入中断不会损坏文件
- **隐私**：控制台执行命令使用世界出生点，不读取在线玩家坐标
